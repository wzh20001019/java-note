#### 1. 配置数据卷

```docker
# 创建容器的命令       宿主机目录(文件):容器内目录(文件)
docker run -it --name=f1 -v /tmp/data:/tmp/data centos:7


# 创建多个数据卷  一个容器对应多个数据卷
docker run -it --name=f2 -v /tmp/data1:/tmp/data1 -v /tmp/data2:/tmp/data2 centos:7
```
