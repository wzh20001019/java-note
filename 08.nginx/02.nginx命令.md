#### 1.nginx 命令

```txt
./nginx -v                      查看nginx版本

./nginx -t                      测试配置文件

./nginx                         启动nginx

./nginx -s stop                 关闭nginx

./nginx -s reload               重启nginx
```
