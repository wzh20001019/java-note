#

#### 1. 构造器

```java
public class Car {
    String name;
    double num;

    // 无参构造器 (!!!)
    public Car() {
        System.out.println("无参构造器");
    }

    // 有参构造器
    public Car(int number) {
        System.out.println("有参构造器");
    }
}
```
